{
  "name": "custom-theme",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "preview": "npm-run-all --parallel sync wpstart ",
    "start": "npm-run-all --parallel",
    "wpstart": "wp-scripts start",
    "sync":"browser-sync start -p 'website.local' --files '**/*.php' 'build/*.js' 'build/*.css'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@wordpress/scripts": "^30.12.0",
    "bootstrap": "^5.3.3",
    "browser-sync": "^3.0.3",
    "npm-run-all": "^4.1.5"
  }
}
